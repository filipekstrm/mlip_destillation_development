[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mlip_distillation"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "fairchem-core==2.21.0",
    "fairchem-data-omat==0.2.0",
]

[tool.setuptools.packages.find]
include = ["mlip_distillation*"]
